services:
  cronometro:
    build: .
    ports:
      - "0.0.0.0:3000:3000"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/app-host
